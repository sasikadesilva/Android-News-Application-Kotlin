<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

        <!-- Disabled state: gray text color -->
        <item android:textColor="@color/edit_text_color" android:state_enabled="false" android:color="@color/edit_text_color" />
        <!-- Enabled state: white text color -->
        <item android:textColor="@color/white" android:state_enabled="true" android:color="@color/white" />
</selector>