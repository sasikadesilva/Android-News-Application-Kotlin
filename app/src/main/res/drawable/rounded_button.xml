<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled state: rounded background with light gray color -->
    <item android:drawable="@drawable/rounded_background_disabled" android:state_enabled="false"/>
    <!-- Enabled state: rounded background with green color -->
    <item android:drawable="@drawable/rounded_background_enabled" android:state_enabled="true"/>
</selector>